#ifndef _X_PIXMAP_H
#define _X_PIXMAP_H

#include <QPixmap>

class XPixmap{
public:
	static QPixmap add(const QPixmap& basePixmap, const QPixmap& addPixmap,Qt::Alignment alignment=0) 
	{
		return QPixmap();
	}

private:
};

#endif //_X_PIXMAP_H
